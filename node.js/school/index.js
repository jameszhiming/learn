var klass=require('./class');
exports.add=function(klasses){
	klasses.forEach(function(item,index){
		var a=item;
		var tname=item.tname;
		var sname=item.sname;
		klass.add('tname,sname)
	})
}
klass.add('scott',['bai','gao'])
